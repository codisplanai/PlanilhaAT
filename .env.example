# ==============================================================================
# PlanAut - Docker / CloudPanel / Dockge / Portainer
# Copie para .env no host. Nunca versione o .env real.
#
# Produção pública (frontend Vercel): https://planaut.codisplan.com.br
# Desenvolvimento público (frontend Vercel): https://planaut.dev.codisplan.com.br
# Backend recomendado:
#   produção:      https://api.planaut.codisplan.com.br
#   desenvolvimento: https://api.planaut.dev.codisplan.com.br
# ===============================================================================

# Stack / imagem ---------------------------------------------------------------
COMPOSE_PROJECT_NAME=planaut-production
PLANAUT_IMAGE=ghcr.io/codisplanai/planilhaat:latest
APP_CONTAINER_NAME=planaut-app

# ÚNICA porta publicada pelo compose. Banco e ferramentas não publicam portas.
# CloudPanel -> Reverse Proxy -> http://127.0.0.1:8000
PLANAUT_BIND_ADDRESS=127.0.0.1
PLANAUT_PORT=8000

# URLs operacionais (referência para painel/DNS/proxy) -------------------------
PLANAUT_PUBLIC_URL=https://planaut.codisplan.com.br
PLANAUT_API_PUBLIC_URL=https://api.planaut.codisplan.com.br
PLANAUT_DEVELOPMENT_URL=https://planaut.dev.codisplan.com.br
PLANAUT_DEVELOPMENT_API_URL=https://api.planaut.dev.codisplan.com.br
CLOUDPANEL_UPSTREAM=http://127.0.0.1:8000

# Aplicação --------------------------------------------------------------------
APP_NAME=PlanAut-Backend
APP_ENV=production
DEBUG=false
ENABLE_LOCAL_AUTH=false
AUTO_CREATE_SCHEMA=false
SEED_DEFAULTS=false
RUN_MIGRATIONS_ON_START=true
WAIT_FOR_DATABASE=true
DATABASE_WAIT_ATTEMPTS=30
DATABASE_WAIT_INTERVAL_SECONDS=2

# Banco ------------------------------------------------------------------------
# Produção recomendada: PostgreSQL/Supabase gerenciado.
DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/postgres

# PostgreSQL local opcional -----------------------------------------------------
# Para usar o banco do próprio compose:
# DATABASE_URL=postgresql://planaut:CHANGE_ME@postgres:5432/planaut
# docker compose --profile local-db up -d
# A imagem PostgreSQL vem do seu GHCR e NÃO publica porta no host.
POSTGRES_IMAGE=ghcr.io/codisplanai/planilhaat-postgres:16-alpine
POSTGRES_CONTAINER_NAME=planaut-postgres
POSTGRES_DB=planaut
POSTGRES_USER=planaut
POSTGRES_PASSWORD=CHANGE_ME_STRONG_PASSWORD

# Supabase ---------------------------------------------------------------------
# SUPABASE_KEY é a chave anon/public. SERVICE_ROLE e JWT_SECRET são privadas.
SUPABASE_URL=https://YOUR_PROJECT.supabase.co
SUPABASE_KEY=YOUR_ANON_PUBLIC_KEY
SUPABASE_SERVICE_ROLE_KEY=YOUR_SERVICE_ROLE_KEY
SUPABASE_JWT_SECRET=YOUR_JWT_SECRET
SUPABASE_STORAGE_BUCKET_TEMPLATES=templates
SUPABASE_STORAGE_BUCKET_OUTPUTS=outputs

# HTTP / CORS ------------------------------------------------------------------
# Frontends Vercel autorizados. Acrescente outros hosts somente quando necessário.
CORS_ORIGINS=https://planaut.codisplan.com.br,https://planaut.dev.codisplan.com.br

# Proteção de upload ------------------------------------------------------------
MAX_UPLOAD_FILE_BYTES=20971520
MAX_UPLOAD_TOTAL_BYTES=104857600
MAX_ZIP_ENTRIES=1000
MAX_ZIP_UNCOMPRESSED_BYTES=104857600
MAX_ZIP_COMPRESSION_RATIO=100

# Logs / limpeza ---------------------------------------------------------------
LOG_MAX_SIZE=10m
LOG_MAX_FILES=3
PRUNE_AFTER_HOURS=2

# Deploy SSH GitHub Actions -----------------------------------------------------
# Configurar em GitHub > Settings > Environments/Secrets, não neste arquivo real:
# DEPLOY_HOST
# DEPLOY_USER
# DEPLOY_PORT=22
# DEPLOY_SSH_KEY
# DEPLOY_KNOWN_HOSTS
# Repository/Environment variable: DEPLOY_PATH=/opt/planaut

# ------------------------------------------------------------------------------
# DESENVOLVIMENTO DOCKER
# ------------------------------------------------------------------------------
# Se development rodar em OUTRO host, pode manter PLANAUT_PORT=8000.
# Se production e development rodarem simultaneamente no MESMO host, TCP exige
# portas de loopback distintas; cada stack continua expondo somente UMA porta.
# Exemplo para o segundo stack:
# COMPOSE_PROJECT_NAME=planaut-development
# PLANAUT_IMAGE=ghcr.io/codisplanai/planilhaat:develop
# APP_CONTAINER_NAME=planaut-dev-app
# APP_ENV=development
# PLANAUT_PORT=8001
# PLANAUT_PUBLIC_URL=https://planaut.dev.codisplan.com.br
# PLANAUT_API_PUBLIC_URL=https://api.planaut.dev.codisplan.com.br
# CLOUDPANEL_UPSTREAM=http://127.0.0.1:8001
# CORS_ORIGINS=https://planaut.dev.codisplan.com.br

# Portainer/Dockge --------------------------------------------------------------
# Use este mesmo compose.yaml e importe/cole as variáveis acima no Environment.
# Não é necessário publicar PostgreSQL, migration tool ou qualquer outra porta.
